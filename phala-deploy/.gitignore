# phala-deploy local secrets/state (NEVER COMMIT)
secrets/
.onboarding/
backup/

# local rollout env (operator-specific)
.env.rollout
.env.rollout-targets

# local tarball artifact (built by build-openclaw.sh)
openclaw.tgz

# controller build artifacts
controller/dist/
controller/node_modules/

# local mux e2e state
local-mux-e2e/.env.local
local-mux-e2e/state/
